meta {
  name: Get dashboard summary
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/dashboard/summary?fromDate={{fromDate}}&toDate={{toDate}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}
