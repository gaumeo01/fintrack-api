meta {
  name: Get dashboard trend by MONTH
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/dashboard/trend?fromDate={{fromDate}}&toDate={{toDate}}&groupBy=MONTH
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}
